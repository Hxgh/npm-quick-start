import React, { FC } from 'react';
import Quick from '../../../src';
import './styles.less';

const Expample: FC = () => {
  return (
    <div className="expample">
      <Quick />
    </div>
  );
};

export default Expample;
